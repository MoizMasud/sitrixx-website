---
import MainLayout from '../../layouts/main.astro';
import AdminLayout from '../../components/AdminLayout';
import MobileUsersPage from '../../components/MobileUsersPage';
import { baseUrl } from '../../lib/base-url';
---

<MainLayout title="Mobile Users - Admin - Sitrixx">
  <AdminLayout activePage="users" client:only="react">
    <MobileUsersPage client:only="react" />
  </AdminLayout>

  <!-- Authentication Guard Script -->
  <script define:vars={{ baseUrl }}>
    const token = localStorage.getItem('sitrixx_token');
    if (!token) {
      window.location.href = baseUrl + '/admin/login';
    }
  </script>

  <!-- Initialize Supabase from npm package -->
  <script>
    import '../../lib/supabase-client';
    console.log('✅ Supabase client initialized on users page');
  </script>
</MainLayout>
